Для вас ничего не меняется — приложение работает ровно как прежде.

Две нативные библиотеки больше не несут build id, менявшийся при каждой сборке. Отпечаток «sha1» линкер считает от входа линковки, а не от самого файла, поэтому он различался у сборок, дававших идентичные библиотеки — и только из-за него сборка F-Droid не совпадала с нашей байт в байт.

Полный список изменений: https://github.com/shelad3/ncx-tunnel/blob/main/CHANGELOG.md
