{
  "$schema_comment": "NCX Tunnel donation methods shown in About -> Support. kind: crypto — address row with copy + pay button (url = wallet deeplink); link — simple link button. note — optional explanation. title/note are brand/network names and are not translated. Array order = popup order. Remote source: docs/donate.json in this repo (updatable without a release); this bundled copy is the offline first-run fallback.",
  "methods": []
}
