<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- specialUse FGS-type появился в Android 14 (API 34). На младших
         API permission не запрашивается — minSdkVersion="34" гейтит его
         только для устройств где он реально что-то значит. -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        android:minSdkVersion="34" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- §050 / F12.3 readWIFIState: sing-box DNS/routing rules с условиями
         `wifi_ssid:` / `wifi_bssid:` требуют чтения текущего WiFi SSID/BSSID
         через `WifiManager.getConnectionInfo()`. На API 29+ этот call требует
         ACCESS_BACKGROUND_LOCATION runtime grant; на API 28- — ACCESS_FINE_LOCATION.
         Без grant `getConnectionInfo()` бросает SecurityException → propagates
         через JNI в Go runtime → process abort. Permissions decl'арены как
         optional — юзер должен grant'нуть через Settings если использует
         wifi_ssid rules в config. См. spec/050 findings. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Android 13+ (API 33+): без NEARBY_WIFI_DEVICES `WifiInfo.ssid`
         возвращает "<unknown ssid>" даже если ACCESS_FINE_LOCATION granted.
         Для wifi_ssid/wifi_bssid правил sing-box нужно реальное имя. -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- §372 Android TV. Обе фичи `required="false"` — телефоны и планшеты
         совместимость не теряют, а TV перестаёт считаться несовместимым:
         `android.hardware.touchscreen` по умолчанию подразумевается
         обязательным, и без явного `false` устройство без тачскрина
         отсеивается. `leanback` не требуем — приложение работает и без
         TV-стека. Иконку в TV-лаунчере даёт LEANBACK_LAUNCHER-категория
         в intent-filter MainActivity (ниже). -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- §375 QR-сканер. `required="false"` обязателен: иначе приложение
         станет формально несовместимым с устройствами без камеры — а §372
         декларирует Android TV как поддерживаемый (best-effort) таргет.
         Там пункт «Scan QR code» прячется по hasCamera(). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:label="@string/app_name"
        android:name=".vpn.BoxApplication"
        android:icon="@mipmap/ic_launcher"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- §372 — TV-лаунчер перечисляет только активити с этой
                     категорией; без неё иконки на телевизоре нет вообще. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <!-- Long-press на QS-плитке открывает приложение: Android при долгом
                 нажатии ищет в пакете activity с этим действием и запускает её. -->
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES"/>
            </intent-filter>
        </activity>

        <service
            android:name=".vpn.BoxVpnService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <!-- Android 14+ требует обоснование для foregroundServiceType="specialUse"
                 через PROPERTY_SPECIAL_USE_FGS_SUBTYPE — читается Play Store reviewer'ами,
                 в Android 15+ может показываться юзеру в системных FGS-настройках.
                 Стандартные FGS-типы (dataSync / mediaPlayback / connectedDevice)
                 не покрывают мультипротокольный VPN-стек с rule-based routing'ом. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn_tunnel: managed multi-protocol VPN (VLESS / VMess / Trojan / Hysteria2 / WireGuard / NaïveProxy / Shadowsocks / SOCKS / SSH / TUIC) with rule-based routing engine." />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <service
            android:name=".vpn.NcxTileService"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".vpn.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- §279 Phase 6 — смена системной локали: при app_language=system
             перештамповать нативные поверхности (канал / шторка / shortcuts /
             тайл / локаль ядра), даже когда Flutter-движок мёртв.
             LOCALE_CHANGED — в exempt-списке implicit-broadcast'ов (API 26+),
             manifest-receiver легален; система шлёт и в non-exported. -->
        <receiver
            android:name=".vpn.LocaleChangedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- §047 Public Intent API — приём automation-команд (Tasker /
             Macrodroid). `enabled="false"` по умолчанию: компонент включается
             рантаймом через PackageManager.setComponentEnabledSetting когда
             юзер поднимает мастер-toggle «Принимать команды автоматизации» —
             это и есть единственный барьер приёма. `android:permission` НЕ
             задан намеренно: per-app пропуск удалён (§157 —
             `checkCallingPermission` в broadcast-onReceive недетерминирован). -->
        <receiver
            android:name=".vpn.NcxIntentReceiver"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="com.nativecodex.ncxtunnel.START_VPN" />
                <action android:name="com.nativecodex.ncxtunnel.STOP_VPN" />
                <action android:name="com.nativecodex.ncxtunnel.TOGGLE_VPN" />
                <action android:name="com.nativecodex.ncxtunnel.SWITCH_NODE" />
                <action android:name="com.nativecodex.ncxtunnel.SET_GROUP" />
                <action android:name="com.nativecodex.ncxtunnel.REBUILD_CONFIG" />
                <action android:name="com.nativecodex.ncxtunnel.REFRESH_SUBS" />
                <action android:name="com.nativecodex.ncxtunnel.RESET_NETWORK" />
                <action android:name="com.nativecodex.ncxtunnel.URLTEST_GROUP" />
            </intent-filter>
        </receiver>

        <!-- §047 Шаг 2 — Locale / Tasker plugin-стандарт (twofortyfouram).
             NCX Tunnel виден в Tasker как Plugin (Action + State). Edit-Activity
             exported (хост открывает их). Fire/Query receiver'ы БЕЗ
             `android:permission` (стандарт запрещает — хост проверяет права
             сам); `enabled="false"` — включаются мастер-toggle'ом
             «Принимать команды автоматизации» вместе с raw-receiver Шага 1. -->
        <!-- «Custom…» — экран выбора остальных команд (switch-node, set-group,
             url-test, refresh, rebuild, reset) + значение extra. -->
        <activity
            android:name=".automation.LocaleSettingEditActivity"
            android:exported="true"
            android:label="@string/label_plugin_custom"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/LaunchTheme">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity>

        <!-- Частые команды — отдельными one-tap строками в списке плагинов
             (как у VPN Hotspot). Host открывает alias по EDIT_SETTING, activity
             headless'ом сразу отдаёт готовый bundle и finish (UI не рисует). -->
        <activity
            android:name=".automation.LocaleQuickActionActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay" />
        <activity-alias
            android:name=".automation.LocaleStartAlias"
            android:targetActivity=".automation.LocaleQuickActionActivity"
            android:exported="true"
            android:label="@string/label_plugin_start"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".automation.LocaleStopAlias"
            android:targetActivity=".automation.LocaleQuickActionActivity"
            android:exported="true"
            android:label="@string/label_plugin_stop"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".automation.LocaleToggleAlias"
            android:targetActivity=".automation.LocaleQuickActionActivity"
            android:exported="true"
            android:label="@string/label_plugin_toggle"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity-alias>
        <receiver
            android:name=".automation.LocaleSettingReceiver"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".automation.LocaleConditionEditActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:theme="@style/LaunchTheme">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_CONDITION" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".automation.LocaleConditionReceiver"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.QUERY_CONDITION" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
